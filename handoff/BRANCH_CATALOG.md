# Branch Catalog
Snapshot taken from commit `630f017` (main) while preparing `handoff/final-week4`.

| Branch | Head Commit | Purpose | Status & Notes | Recommended Action |
| ------ | ----------- | ------- | -------------- | ------------------ |
| `main` | `630f017` | Production-ready code (Pages + Render) | Contains latest docs, launch checklist, and backend stability fixes. | Use as deploy source. Keep protected with PR reviews. |
| `dev` | `209e6d2` | Historical integration branch for feature work | Missing recent documentation (launch checklist, logs) and reverts several UX improvements. | Do **not** merge to main without rebasing and restoring removed assets. Prefer creating new feature branches from `main`. |
| `staging` | `ce59dcc` | Staging merge gate | Mirrors `main` but lacks newest documentation PDFs. Good candidate for pre-prod testing. | Update from `main` post-handoff if staging workflow continues. |
| `security/rate-limiting` | `09ec54e` | Experiment to tighten security & restructure docs | Diverges significantly: removes many docs, swaps to mock API client, adds OWASP ZAP workflow. | Cherry-pick specific hardening commits as needed; avoid full merge. Review `.github/workflows/zap-baseline.yml`. |
| `feature/docs-week4-log` | `196bd78` | Attempt to streamline docs + ownership changes | Removes documentation READMEs and adjusts backend event ownership logic. Superseded by work already merged into `main`. | Consider closed; reference only for historical context. |
| `feature/marketing-playbooks` | `3de3a59` | Adds comprehensive marketing/SEO playbooks & deliverables | Source for `Marketing/` directory (now merged into handoff branch). Contains PDF exports and render script. | Already incorporated; keep branch for reference or delete after verifying assets in `main`. |
| `origin/gh-pages` | (generated) | GitHub Pages build artefacts | Auto-generated by Actions; contains compiled frontend bundle. | No manual changes; redeploy by pushing to `main`. |

### Branch Workflow Expectations
- Create future feature branches from `main` unless a staged integration workflow is reinstated.
- Use PR reviews + automated checks (npm audit, deploy workflow) before merges.
- Clean up long-lived branches quarterly as described in `handoff/MAINTENANCE_PLAN.md`.

> Update this catalog whenever new long-lived branches are created or retired so incoming maintainers have an accurate map of the repo.

